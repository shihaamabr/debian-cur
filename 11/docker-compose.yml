version: '3.5'
services:
#########################
  debian11-x86:
    build: .
    hostname: debian11-x86
    platform: linux/amd64/v3
    image: debian11-curl
  debian11-arm:
    build: .
    hostname: debian11-arm
    platform: linux/arm64
    image: debain11-curl
